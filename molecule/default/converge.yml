---
- name: Converge
  hosts: all
  become: yes

  vars:
    metadata_service: consul

    consul_enabled: true
    consul_version: "1.20.5-1"
    consul_node_role: server
    consul_telemetry_enabled: true

  roles:
    - role: wazo.consul
